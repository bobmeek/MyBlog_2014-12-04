<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.myblog.dao.facade.IUsersDao">

	<resultMap id="usersResultMap" type="UsersVO">
		<id property="id" column="id" />
		<result property="username" column="username" />
		<result property="userpwd" column="userpwd" />
		<result property="mail" column="mail" />
		<result property="phone" column="phone" />
		<result property="sex" column="sex" />
		<result property="birthday" column="birthday" />
	</resultMap>

	<select id="getUsersAll" parameterType="int" resultMap="usersResultMap">
		SELECT *
		FROM users
	</select>
	<insert id="addUser" parameterType="org.myblog.model.UsersVO" useGeneratedKeys="true" keyProperty="id">
	    insert into users(username,userpwd,mail,phone,sex,birthday)
	    values(#{username},#{userpwd},#{mail},#{phone},#{sex},#{birthday})
	</insert>


</mapper>